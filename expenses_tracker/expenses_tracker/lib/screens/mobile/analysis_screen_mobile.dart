import 'package:flutter/material.dart';

class AnalysisScreenMobile extends StatelessWidget {
  const AnalysisScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}