import 'package:flutter/material.dart';

class FilterTransactionMobile extends StatelessWidget {
  const FilterTransactionMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mobile Filter Transaction'),
    );
  }
}