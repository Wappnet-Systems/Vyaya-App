import 'package:flutter/material.dart';

class DesktopUserDetails extends StatelessWidget {
  const DesktopUserDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Title'),
      ),
    );
  }
}