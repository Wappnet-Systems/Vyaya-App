import 'package:flutter/material.dart';

class DetailHomeScreenDesktop extends StatelessWidget {
  const DetailHomeScreenDesktop ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Desktop Home'),
    );
  }
}