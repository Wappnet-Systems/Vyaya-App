import 'package:flutter/material.dart';

class FilterTransactionDesktop extends StatelessWidget {
  const FilterTransactionDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mobile Filter Transaction'),
    );
  }
}