import 'package:flutter/material.dart';

class AnalysisScreenDesktop extends StatelessWidget {
  const AnalysisScreenDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}